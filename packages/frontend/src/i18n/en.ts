import { TranslationSchema } from "./TranslationSchema";

const en: TranslationSchema = {
  misc: {
    appearance: `Appearance`,
    language: `Language`,
    settings: `Settings`
  },
  failed: `failed_en`,
  success: `success_en`
}

export default en;